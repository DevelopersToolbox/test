name: 'build'
description: 'build stuff'

inputs:
  os-name:
    description: “The os name”
    required: true
  version:
    description: “the os version”
    required: true

runs:

  using: "composite"
  steps:
    - name: Set up Docker
      uses: docker-practice/actions-setup-docker@master
    - name: Echo OS Name
      run: echo ${{ inputs.os-name }}
      shell: bash
    - name: Echo Version
      run: echo ${{ inputs.version }}
      shell: bash
