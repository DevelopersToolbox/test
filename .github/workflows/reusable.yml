name: reusable
on:
  push:

jobs:
  build-alpine:
    name: Alpine Linux
    uses: actionstoolbox/container-framework-actions/.github/workflows/build-containers.yml@master
    with:
      directory: alpine
      os-name: Alpine Linux
