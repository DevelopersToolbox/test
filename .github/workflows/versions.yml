name: versions
on:
  push:

jobs:
  get-versions:
    runs-on: ubuntu-latest
    name: Get OS versions
    outputs:
      amazonlinux-versions: ${{steps.versions.outputs.amazonlinux-versions}}
      archlinux-versions: ${{steps.versions.outputs.archlinux-versions}}
      centos-versions: ${{steps.versions.outputs.centos-versions}}
      debian-versions: ${{steps.versions.outputs.debian-versions}}
      oraclelinux-versions: ${{steps.versions.outputs.oraclelinux-versions}}
      photon-versions: ${{steps.versions.outputs.photon-versions}}
      scientificlinux-versions: ${{steps.versions.outputs.scientificlinux-versions}}
      ubuntu-versions: ${{steps.versions.outputs.ubuntu-versions}}
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v2
      - name: Get OS Versions
        id: foo
        uses: actionstoolbox/container-framework-actions/actions/get-versions-all@master
  alpine:
    runs-on: ubuntu-latest
    needs: get-versions
    strategy:
      fail-fast: false
      matrix:
        version: ${{fromJson(needs.get-versions.outputs.alpine-versions)}}
    name: Build Test on Alpine Linux ${{ matrix.version }}
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v2
      - name: Build Containers
        uses: ./.github/workflows/composite/build
        with:
          os-name: 'Alpine Linux'
          version: ${{ matrix.version }}
  centos:
    runs-on: ubuntu-latest
    needs: get-versions
    strategy:
      fail-fast: false
      matrix:
        version: ${{fromJson(needs.get-versions.outputs.centos-versions)}}
    name: Build Test on Centos ${{ matrix.version }}
    steps:
      - uses: actions/checkout@v2
      - uses: ./.github/workflows/composite/build
        with:
          os-name: 'Centos'
          version: ${{ matrix.version }}
  debian:
    runs-on: ubuntu-latest
    needs: get-versions
    strategy:
      fail-fast: false
      matrix:
        version: ${{fromJson(needs.get-versions.outputs.debian-versions)}}
    name: Build Test on Debian ${{ matrix.version }}
    steps:
      - uses: actions/checkout@v2
      - uses: ./.github/workflows/composite/build
        with:
          os-name: 'Debian'
          version: ${{ matrix.version }}
  photon:
    runs-on: ubuntu-latest
    needs: get-versions
    strategy:
      fail-fast: false
      matrix:
        version: ${{fromJson(needs.get-versions.outputs.photon-versions)}}
    if: ${{ needs.get-versions.outputs.photon-versions != '[]' }}
    name: Build Test on Photon ${{ matrix.version }}
    steps:
      - uses: actions/checkout@v2
      - uses: ./.github/workflows/composite/build
        with:
          os-name: 'Photon'
          version: ${{ matrix.version }}
